spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: localhost
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-log.yaml # 共享日志配置