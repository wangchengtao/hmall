server:
  port: 8080

hm:
  jwt:
    location: classpath:hmall.jks
    alias: hmall # 秘钥别名
    password: hmall123
    tokenTTL: 30m
  auth:
    excludePaths:
      - /search/**
      - /users/login
      - /items/**
